node_modules
dist
.vite
*.log

# Hero video master (the 146 MB pre-compression source). The served
# file `assets/hero.mp4` is now ~320 KB (compressed by
# `scripts/compress-hero.mjs`) and is tracked in git so deploys are
# reproducible. Master stays ignored — re-run the compress script
# locally if you want to re-encode with different settings.
assets/hero.original.mp4
assets/hero.webm
