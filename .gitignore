node_modules
dist
out
*.log
.DS_Store
.env
.env.local
.pnpm-store
coverage
_reference

# Local-only assistant memory + state. Not part of the public project.
.claude/

# Tesseract OCR data file — large binary, downloaded as a build dep.
# Kept out so the repo stays small; consumers fetch it on first install
# if they actually use OCR-dependent features.
eng.traineddata
